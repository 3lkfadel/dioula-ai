# === Core ML (Linux / AWS GPU) ===
torch>=2.3.0
accelerate>=0.30.0

# === HuggingFace ===
transformers>=4.45.0
datasets>=2.20.0
huggingface_hub>=0.24.0
peft>=0.11.0
trl>=0.9.0              # SFTTrainer pour le fine-tuning supervisé

# === QLoRA 4-bit (Llama 3.1 70B) ===
bitsandbytes>=0.43.0    # Quantization 4-bit / 8-bit GPU
scipy>=1.11.0           # dépendance bitsandbytes

# === Data processing ===
pandas>=2.0.0
numpy>=1.26.0
tqdm>=4.66.0

# === Métriques ===
sacrebleu>=2.4.0        # BLEU score pour évaluation traduction
rouge-score>=0.1.2

# === API ===
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# === Utils ===
python-dotenv>=1.0.0
sentencepiece>=0.2.0    # Tokenizer Llama
