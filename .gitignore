# Modèles (trop lourds pour GitHub — stocker sur S3 ou HuggingFace Hub)
model/
*.safetensors
*.bin
*.gguf

# Dataset brut (remplacé par dataset_clean.json)
dataset_raw.json

# Splits JSONL (régénérables via : python3 2_prepare_dataset.py)
dataset_train.jsonl
dataset_valid.jsonl
dataset_test.jsonl
train.jsonl
valid.jsonl
test.jsonl

# Environnement virtuel
env/
venv/
__pycache__/
*.pyc
*.pyo
.DS_Store

# Résultats de benchmark (générés à l'exécution)
benchmark_results.json

# Cache HuggingFace
.cache/
